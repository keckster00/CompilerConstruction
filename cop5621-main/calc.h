extern long calculateNum(struct statment_value* first, struct statment_value* second, int operator);
extern int calculateFla(struct statment_value* first, struct statment_value* second, int operator);
